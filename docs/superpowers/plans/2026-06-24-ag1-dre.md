# AG-1 — DRE (regime de caixa) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Exibir um DRE por regime de caixa (receita recebida − despesas pagas por categoria = resultado) na aba Análises Gerenciais.

**Architecture:** Novo módulo `src/modules/analises/` (service + router) com agregação SQL sobre `orcamentos` (receita) e `contas_pagar` (despesas). Frontend: bloco DRE em `#page-analises` do `dashboard.html` com seletor de período + KPIs + despesas por categoria. Sem migration.

**Tech Stack:** Node.js/Express, PostgreSQL (`pg`), vanilla-JS dashboard. Sem postgres local — verificação por `node --check` + smoke E2E no VPS.

**Convenções/Fatos verificados:**
- `const db = require('../../db')`; `db.query`. Services retornam objeto ou `{ erro:[...] }`.
- Registro em `src/modules/index.js`: `router.use('/<nome>', requireAuthApi, require('./<nome>/router'))` (ver linhas de `maquinas`/`entradas`/`contas-pagar`).
- `orcamentos`: `total NUMERIC`, `pago BOOLEAN DEFAULT false`, `pago_em TIMESTAMPTZ`, `status_pagamento`.
- `contas_pagar`: `valor NUMERIC`, `tipo_despesa` (enum), `status` ('pendente'|'agendado'|'pago'|'vencido'|'cancelado'), `pago_em TIMESTAMP`, `vencimento DATE`.
- VPS: root@2.25.147.243, /var/www/lkl-chatbot, DB `lkl_chatbot`. Deploy via rsync; restart `pm2 restart lkl-chatbot --update-env`; node server-side `node -r dotenv/config -e '<js>'`.
- Dashboard: `api(path,opts)` fetch-style (cookie-session); `showPage(id)` + dict `loaders`; helpers `escHtml`, `_kpiCard(label,valor,cor)` (já existe, criado no painel NF-e); `#page-analises` é placeholder; NAV já tem `analises`.
- Commits terminam com `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

**File Structure:**
- Create: `src/modules/analises/service.js` — `dre({inicio,fim})`.
- Create: `src/modules/analises/router.js` — `GET /dre`.
- Modify: `src/modules/index.js` — registrar `/analises`.
- Modify: `public/dashboard.html` — bloco DRE em `#page-analises`.

---

### Task 1: Backend — módulo `analises` (service + router) + registro

**Files:**
- Create: `src/modules/analises/service.js`
- Create: `src/modules/analises/router.js`
- Modify: `src/modules/index.js`

- [ ] **Step 1: Criar o service**

Create `src/modules/analises/service.js`:

```js
const db = require('../../db');

function _mesCorrente() {
  const n = new Date();
  const inicio = new Date(n.getFullYear(), n.getMonth(), 1);
  const fim = new Date(n.getFullYear(), n.getMonth() + 1, 0);
  const fmt = d => d.toISOString().slice(0, 10);
  return { inicio: fmt(inicio), fim: fmt(fim) };
}

const DATE_RE = /^\d{4}-\d{2}-\d{2}$/;

async function dre({ inicio, fim } = {}) {
  if (!inicio || !fim) { const m = _mesCorrente(); inicio = inicio || m.inicio; fim = fim || m.fim; }
  if (!DATE_RE.test(inicio) || !DATE_RE.test(fim)) return { erro: ['Datas inválidas (use YYYY-MM-DD)'] };
  if (fim < inicio) return { erro: ['Data final menor que a inicial'] };

  const recR = await db.query(
    `SELECT COALESCE(SUM(total),0) AS receita
     FROM orcamentos WHERE pago = true AND pago_em::date BETWEEN $1 AND $2`,
    [inicio, fim]);
  const receita = Number(recR.rows[0].receita);

  const despR = await db.query(
    `SELECT tipo_despesa AS categoria, COALESCE(SUM(valor),0) AS valor
     FROM contas_pagar WHERE status = 'pago' AND pago_em::date BETWEEN $1 AND $2
     GROUP BY tipo_despesa ORDER BY valor DESC`,
    [inicio, fim]);
  const despesas = despR.rows.map(r => ({ categoria: r.categoria, valor: Number(r.valor) }));
  const total_despesas = despesas.reduce((s, d) => s + d.valor, 0);

  const resultado = receita - total_despesas;
  const margem = receita > 0 ? resultado / receita : 0;

  return { periodo: { inicio, fim }, receita, despesas, total_despesas, resultado, margem };
}

module.exports = { dre };
```

- [ ] **Step 2: Criar o router**

Create `src/modules/analises/router.js`:

```js
const express = require('express');
const { requireRole } = require('../../middleware/auth');
const service = require('./service');

const router = express.Router();

router.get('/dre', requireRole('admin', 'gestor', 'financeiro'), async (req, res) => {
  try {
    const result = await service.dre({ inicio: req.query.inicio, fim: req.query.fim });
    if (result.erro) return res.status(400).json({ errors: result.erro });
    res.json(result);
  } catch (err) { console.error(err); res.status(500).json({ error: 'Erro interno' }); }
});

module.exports = router;
```

- [ ] **Step 3: Registrar em index.js**

Em `src/modules/index.js`, após a linha de `contas-pagar`, adicionar:
```js
router.use('/analises', requireAuthApi, require('./analises/router'));
```

- [ ] **Step 4: node --check**

```bash
node --check src/modules/analises/service.js && node --check src/modules/analises/router.js && node --check src/modules/index.js && echo OK
```
Expected: `OK`.

- [ ] **Step 5: Deploy + smoke**

```bash
rsync -az src/modules/analises/ root@2.25.147.243:/var/www/lkl-chatbot/src/modules/analises/
rsync -az src/modules/index.js root@2.25.147.243:/var/www/lkl-chatbot/src/modules/index.js
ssh root@2.25.147.243 "cd /var/www/lkl-chatbot && pm2 restart lkl-chatbot --update-env >/dev/null 2>&1 && node -r dotenv/config -e \"
const s=require('./src/modules/analises/service');
s.dre({}).then(r=>{ console.log(JSON.stringify(r)); process.exit(0); }).catch(e=>{console.error(e.message);process.exit(1)});
\""
```
Expected: imprime `{ periodo, receita, despesas:[...], total_despesas, resultado, margem }` para o mês atual (valores podem ser 0 se não houver dados — aceitável).

- [ ] **Step 6: Commit**

```bash
git add src/modules/analises/ src/modules/index.js
git commit -m "feat(analises): módulo DRE (regime de caixa) — GET /api/v2/analises/dre

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Frontend — bloco DRE na aba Análises

**Files:**
- Modify: `public/dashboard.html`

- [ ] **Step 1: Investigar**

```bash
grep -n "page-analises\|loaders\|function _kpiCard\|function api\|function escHtml" public/dashboard.html | head
```
Confirmar o markup atual de `#page-analises`, o dict `loaders`, e que `_kpiCard`/`api`/`escHtml` existem.

- [ ] **Step 2: Substituir o conteúdo de `#page-analises`**

Trocar o miolo do `<div class="page" id="page-analises">` por:
```html
<div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;flex-wrap:wrap;gap:8px">
  <div style="display:flex;align-items:center;gap:10px"><span style="font-size:20px">📈</span><h2 style="margin:0">Análises Gerenciais — DRE</h2></div>
  <div style="display:flex;gap:6px;align-items:center;flex-wrap:wrap">
    <button class="btn btn-outline" onclick="drePeriodo('mes')">Mês atual</button>
    <button class="btn btn-outline" onclick="drePeriodo('mes_ant')">Mês anterior</button>
    <button class="btn btn-outline" onclick="drePeriodo('ano')">Ano</button>
    <input id="dre-ini" type="date" style="width:140px">
    <input id="dre-fim" type="date" style="width:140px">
    <button class="btn btn-primary" onclick="dreAplicar()">Aplicar</button>
  </div>
</div>
<div id="dre-kpis" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:12px;margin-bottom:16px"></div>
<div id="dre-despesas"></div>
```

- [ ] **Step 3: Registrar o loader**

No dict `loaders` de `showPage`, adicionar:
```js
    analises: () => loadAnalisesMain(),
```

- [ ] **Step 4: Funções JS**

Adicionar (adaptar helpers reais; reutilizar `_kpiCard`):
```js
function _fmtData(d) { return d.toISOString().slice(0,10); }
function drePeriodo(tipo) {
  const n = new Date(); let ini, fim;
  if (tipo === 'mes') { ini = new Date(n.getFullYear(), n.getMonth(), 1); fim = new Date(n.getFullYear(), n.getMonth()+1, 0); }
  else if (tipo === 'mes_ant') { ini = new Date(n.getFullYear(), n.getMonth()-1, 1); fim = new Date(n.getFullYear(), n.getMonth(), 0); }
  else { ini = new Date(n.getFullYear(), 0, 1); fim = new Date(n.getFullYear(), 11, 31); }
  document.getElementById('dre-ini').value = _fmtData(ini);
  document.getElementById('dre-fim').value = _fmtData(fim);
  dreAplicar();
}
async function loadAnalisesMain() {
  if (!document.getElementById('dre-ini').value) { drePeriodo('mes'); return; }
  dreAplicar();
}
async function dreAplicar() {
  const inicio = document.getElementById('dre-ini').value;
  const fim = document.getElementById('dre-fim').value;
  const r = await api(`/api/v2/analises/dre?inicio=${inicio}&fim=${fim}`);
  if (!r || r.errors || r.error) { document.getElementById('dre-kpis').innerHTML = '<p style="color:#c00">Erro ao carregar DRE</p>'; return; }
  renderDre(r);
}
function renderDre(r) {
  const fmt = v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2});
  const corRes = r.resultado >= 0 ? '#2e7d32' : '#c62828';
  document.getElementById('dre-kpis').innerHTML =
    _kpiCard('Receita', fmt(r.receita)) +
    _kpiCard('Despesas', fmt(r.total_despesas), '#e65100') +
    _kpiCard('Resultado', fmt(r.resultado), corRes) +
    _kpiCard('Margem', (r.margem*100).toFixed(1) + '%', corRes);
  const max = Math.max(1, ...r.despesas.map(d => d.valor));
  const linhas = r.despesas.length ? r.despesas.map(d => {
    const pct = r.total_despesas > 0 ? (d.valor / r.total_despesas * 100) : 0;
    const w = (d.valor / max * 100);
    return `<div style="margin-bottom:8px">
      <div style="display:flex;justify-content:space-between;font-size:13px;margin-bottom:3px">
        <span>${escHtml(d.categoria)}</span>
        <span style="color:#555">${fmt(d.valor)} · ${pct.toFixed(1)}%</span>
      </div>
      <div style="background:#f0f0f0;border-radius:6px;height:8px;overflow:hidden">
        <div style="background:#e65100;height:8px;width:${w}%"></div>
      </div></div>`;
  }).join('') : '<p style="color:#999;padding:12px">Sem despesas no período</p>';
  document.getElementById('dre-despesas').innerHTML = `
    <div style="background:#fff;border:0.5px solid #e8eaf6;border-radius:12px;padding:14px 16px">
      <div style="font-weight:700;color:#3949ab;font-size:13px;margin-bottom:10px">DESPESAS POR CATEGORIA</div>
      ${linhas}
      <div style="border-top:0.5px solid #eee;margin-top:10px;padding-top:10px;display:flex;justify-content:space-between;font-weight:600">
        <span>Receita ${fmt(r.receita)} − Despesas ${fmt(r.total_despesas)}</span>
        <span style="color:${corRes}">= ${fmt(r.resultado)}</span>
      </div>
    </div>`;
}
```

- [ ] **Step 5: Deploy + verificação**

```bash
rsync -az public/dashboard.html root@2.25.147.243:/var/www/lkl-chatbot/public/dashboard.html
```
No painel: aba Análises Gerenciais → KPIs do DRE carregam; trocar período (Mês atual/anterior/Ano/Personalizado) recalcula; despesas por categoria com barras. Reportar o que viu.

- [ ] **Step 6: Commit**

```bash
git add public/dashboard.html
git commit -m "feat(analises): bloco DRE no painel (seletor de período + KPIs + despesas por categoria)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Atualizar memória do projeto

**Files:**
- Modify: `/Users/klebercamara/.claude/projects/-Users-klebercamara-LKL/memory/project_sprint_status.md`

- [ ] **Step 1: Registrar**

Anotar: "AG-1 DRE: CONCLUÍDO — 2026-06-24. Módulo /api/v2/analises (GET /dre?inicio=&fim=, regime de caixa: receita=orcamentos pago+pago_em no período; despesas=contas_pagar status=pago+pago_em agrupadas por tipo_despesa; resultado e margem). Bloco DRE na aba Análises (seletor período + KPIs + despesas por categoria com barras). Sem migration. Próximos blocos da aba Análises: AG-2 Fluxo de Caixa, AG-3 Metas, AG-4 Insights IA."

- [ ] **Step 2: Sem commit** (memória fora do git).

---

## Notas de verificação final

- Confirmar que `pago_em` está realmente preenchido nos orçamentos pagos (o `confirmarPagamento` seta `pago=true` + `pago_em`); se algum pago antigo tiver `pago_em` nulo, ele não entra no período — comportamento aceitável para o DRE de caixa.
- Valores monetários sempre via `toLocaleString('pt-BR',{minimumFractionDigits:2})`; margem com 1 casa.
