# AG-3 — Metas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cadastrar a meta mensal de faturamento da empresa e acompanhar o realizado (orçamentos aprovados no mês) com percentual atingido, no bloco "Metas" da aba Análises.

**Architecture:** Tabela `metas` (migration 039). Estende o módulo `src/modules/analises/` com `salvarMeta`/`metaMes` + rotas. Frontend: nova pill "Metas" na sub-nav da aba Análises com seletor mês/ano, input de meta e cards + barra de progresso.

**Tech Stack:** Node.js/Express, PostgreSQL (`pg`), vanilla-JS dashboard. Sem postgres local — `node --check` + smoke E2E no VPS.

**Convenções/Fatos verificados:**
- Migrations em `sql/migrations/NNN_*.sql`. Próximo número livre: **039**. Tabela NOVA → criável pelo user da app; se erro de permissão, `sudo -u postgres psql -d lkl_chatbot`.
- `src/modules/analises/service.js` exporta `{ dre, fluxoCaixa }`; `router.js` tem `GET /dre` e `GET /fluxo-caixa`. Módulo em `/api/v2/analises`.
- `orcamentos`: `total`, `aprovado_em TIMESTAMPTZ` (setado em `aprovar()`), `status`.
- VPS: root@2.25.147.243, /var/www/lkl-chatbot, DB `lkl_chatbot`. Deploy rsync; restart `pm2 restart lkl-chatbot --update-env`; node `node -r dotenv/config -e '<js>'`.
- Dashboard: aba Análises tem sub-nav em pills `analisesSub('dre'|'fluxo')` com seções `#ag-dre`/`#ag-fluxo`; botões `#agtab-dre`/`#agtab-fluxo`. Helpers `api`, `_kpiCard`, `escHtml`.
- Commits terminam com `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

**File Structure:**
- Create: `sql/migrations/039_metas.sql`
- Modify: `src/modules/analises/service.js` — `salvarMeta`, `metaMes`.
- Modify: `src/modules/analises/router.js` — `GET/POST /metas`.
- Modify: `public/dashboard.html` — pill Metas + bloco.

---

### Task 1: Migration 039 — tabela `metas`

**Files:**
- Create: `sql/migrations/039_metas.sql`

- [ ] **Step 1: Escrever a migration**

Create `sql/migrations/039_metas.sql`:
```sql
-- AG-3: meta mensal de faturamento da empresa
CREATE TABLE IF NOT EXISTS metas (
  id          SERIAL PRIMARY KEY,
  ano         INTEGER NOT NULL,
  mes         INTEGER NOT NULL CHECK (mes BETWEEN 1 AND 12),
  valor_meta  NUMERIC(12,2) NOT NULL,
  updated_at  TIMESTAMPTZ DEFAULT now(),
  UNIQUE (ano, mes)
);
```

- [ ] **Step 2: Aplicar no VPS**

```bash
rsync -az sql/migrations/039_metas.sql root@2.25.147.243:/var/www/lkl-chatbot/sql/migrations/039_metas.sql
ssh root@2.25.147.243 "cd /var/www/lkl-chatbot && psql \$DATABASE_URL -f sql/migrations/039_metas.sql"
```
Se der erro de permissão, rode: `ssh root@2.25.147.243 "sudo -u postgres psql -d lkl_chatbot -f /var/www/lkl-chatbot/sql/migrations/039_metas.sql"`.
Expected: `CREATE TABLE` sem erro.

- [ ] **Step 3: Verificar**

```bash
ssh root@2.25.147.243 "cd /var/www/lkl-chatbot && psql \$DATABASE_URL -c '\d metas'"
```
Expected: tabela `metas` com colunas id/ano/mes/valor_meta/updated_at e UNIQUE(ano,mes).

- [ ] **Step 4: Commit**

```bash
git add sql/migrations/039_metas.sql
git commit -m "feat(analises): migration 039 — tabela metas

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Backend — `salvarMeta` + `metaMes` + rotas

**Files:**
- Modify: `src/modules/analises/service.js`
- Modify: `src/modules/analises/router.js`

- [ ] **Step 1: Adicionar funções ao service**

Em `src/modules/analises/service.js`, antes do `module.exports`, adicionar:
```js
function _intervaloMes(ano, mes) {
  const ini = new Date(ano, mes - 1, 1);
  const fim = new Date(ano, mes, 0);
  const fmt = d => d.toISOString().slice(0, 10);
  return { inicio: fmt(ini), fim: fmt(fim) };
}

async function salvarMeta({ ano, mes, valor }) {
  ano = parseInt(ano); mes = parseInt(mes); valor = parseFloat(valor);
  if (!Number.isInteger(ano) || ano < 2000 || ano > 2100) return { erro: ['Ano inválido'] };
  if (!Number.isInteger(mes) || mes < 1 || mes > 12) return { erro: ['Mês inválido (1-12)'] };
  if (!(valor >= 0)) return { erro: ['Valor da meta inválido'] };
  const r = await db.query(
    `INSERT INTO metas (ano, mes, valor_meta) VALUES ($1,$2,$3)
     ON CONFLICT (ano, mes) DO UPDATE SET valor_meta = EXCLUDED.valor_meta, updated_at = now()
     RETURNING *`, [ano, mes, valor]);
  return { meta: r.rows[0] };
}

async function metaMes({ ano, mes } = {}) {
  const n = new Date();
  ano = ano ? parseInt(ano) : n.getFullYear();
  mes = mes ? parseInt(mes) : (n.getMonth() + 1);
  if (!Number.isInteger(mes) || mes < 1 || mes > 12) return { erro: ['Mês inválido (1-12)'] };
  const { inicio, fim } = _intervaloMes(ano, mes);
  const mR = await db.query('SELECT valor_meta FROM metas WHERE ano=$1 AND mes=$2', [ano, mes]);
  const meta = mR.rows[0] ? Number(mR.rows[0].valor_meta) : null;
  const rR = await db.query(
    `SELECT COALESCE(SUM(total),0) AS realizado FROM orcamentos
     WHERE aprovado_em::date BETWEEN $1 AND $2 AND status NOT IN ('cancelado','reprovado')`,
    [inicio, fim]);
  const realizado = Number(rR.rows[0].realizado);
  const percentual = meta && meta > 0 ? realizado / meta : 0;
  return { ano, mes, meta, realizado, percentual };
}
```
E trocar o `module.exports` para:
```js
module.exports = { dre, fluxoCaixa, salvarMeta, metaMes };
```

- [ ] **Step 2: Adicionar as rotas**

Em `src/modules/analises/router.js`, antes de `module.exports = router;`, adicionar:
```js
router.get('/metas', requireRole('admin', 'gestor', 'financeiro'), async (req, res) => {
  try {
    const result = await service.metaMes({ ano: req.query.ano, mes: req.query.mes });
    if (result.erro) return res.status(400).json({ errors: result.erro });
    res.json(result);
  } catch (err) { console.error(err); res.status(500).json({ error: 'Erro interno' }); }
});

router.post('/metas', requireRole('admin', 'gestor', 'financeiro'), async (req, res) => {
  try {
    const result = await service.salvarMeta(req.body || {});
    if (result.erro) return res.status(400).json({ errors: result.erro });
    res.status(201).json(result.meta);
  } catch (err) { console.error(err); res.status(500).json({ error: 'Erro interno' }); }
});
```

- [ ] **Step 3: node --check**

```bash
node --check src/modules/analises/service.js && node --check src/modules/analises/router.js && echo OK
```
Expected: `OK`.

- [ ] **Step 4: Deploy + smoke**

```bash
rsync -az src/modules/analises/ root@2.25.147.243:/var/www/lkl-chatbot/src/modules/analises/
ssh root@2.25.147.243 "cd /var/www/lkl-chatbot && pm2 restart lkl-chatbot --update-env >/dev/null 2>&1 && node -r dotenv/config -e \"
const s=require('./src/modules/analises/service');
const n=new Date(); const ano=n.getFullYear(); const mes=n.getMonth()+1;
(async()=>{
  console.log('salvar:', JSON.stringify(await s.salvarMeta({ano,mes,valor:50000})));
  console.log('consulta:', JSON.stringify(await s.metaMes({ano,mes})));
  process.exit(0);
})().catch(e=>{console.error(e.message);process.exit(1)});
\""
```
Expected: `salvar` retorna a meta gravada (valor_meta 50000); `consulta` retorna `{ ano, mes, meta:50000, realizado:<n>, percentual:<n> }`.

- [ ] **Step 5: Commit**

```bash
git add src/modules/analises/
git commit -m "feat(analises): metas — salvarMeta/metaMes + rotas GET/POST /analises/metas

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Frontend — pill "Metas" + bloco

**Files:**
- Modify: `public/dashboard.html`

- [ ] **Step 1: Investigar a sub-nav atual**

```bash
grep -n "agtab-dre\|agtab-fluxo\|function analisesSub\|id=\"ag-fluxo\"\|id=\"ag-dre\"" public/dashboard.html | head
```
Confirmar a estrutura da sub-nav (pills) e onde fechar `#ag-fluxo` para inserir `#ag-metas` em seguida.

- [ ] **Step 2: Adicionar a pill Metas e a seção**

No sub-menu (junto de DRE/Fluxo), adicionar o botão:
```html
<button id="agtab-metas" class="btn btn-outline" onclick="analisesSub('metas')">Metas</button>
```
Após o fechamento de `<div id="ag-fluxo">…</div>`, adicionar:
```html
<div id="ag-metas" style="display:none">
  <div style="display:flex;align-items:center;gap:10px;margin-bottom:14px;flex-wrap:wrap">
    <select id="meta-mes" onchange="loadMeta()" style="width:130px">
      <option value="1">Janeiro</option><option value="2">Fevereiro</option><option value="3">Março</option>
      <option value="4">Abril</option><option value="5">Maio</option><option value="6">Junho</option>
      <option value="7">Julho</option><option value="8">Agosto</option><option value="9">Setembro</option>
      <option value="10">Outubro</option><option value="11">Novembro</option><option value="12">Dezembro</option>
    </select>
    <input id="meta-ano" type="number" style="width:90px">
    <span style="font-size:13px;color:#777;margin-left:8px">Meta (R$)</span>
    <input id="meta-valor" type="number" step="0.01" style="width:140px">
    <button class="btn btn-primary" onclick="salvarMetaUI()">Salvar</button>
  </div>
  <div id="meta-kpis" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:12px;margin-bottom:12px"></div>
  <div id="meta-barra"></div>
</div>
```

- [ ] **Step 3: Atualizar `analisesSub` para tratar 'metas' + funções**

Localizar `function analisesSub(secao)` e estendê-la para incluir a seção/botão de metas. Substituir o corpo por:
```js
function analisesSub(secao) {
  ['dre','fluxo','metas'].forEach(s => {
    const sec = document.getElementById('ag-' + s);
    const tab = document.getElementById('agtab-' + s);
    if (sec) sec.style.display = (s === secao) ? '' : 'none';
    if (tab) tab.className = 'btn ' + (s === secao ? 'btn-primary' : 'btn-outline');
  });
  if (secao === 'fluxo' && !_fluxoData) loadFluxo();
  if (secao === 'metas') { _initMetaPeriodo(); loadMeta(); }
}
```
Adicionar (reutilizar `_kpiCard`):
```js
function _initMetaPeriodo() {
  const n = new Date();
  const ma = document.getElementById('meta-ano'); if (ma && !ma.value) ma.value = n.getFullYear();
  const mm = document.getElementById('meta-mes'); if (mm && !mm.value) mm.value = String(n.getMonth()+1);
}
async function loadMeta() {
  _initMetaPeriodo();
  const ano = document.getElementById('meta-ano').value;
  const mes = document.getElementById('meta-mes').value;
  const r = await api(`/api/v2/analises/metas?ano=${ano}&mes=${mes}`);
  if (!r || r.errors || r.error) { document.getElementById('meta-kpis').innerHTML = '<p style="color:#c00">Erro ao carregar meta</p>'; return; }
  document.getElementById('meta-valor').value = r.meta != null ? r.meta : '';
  renderMeta(r);
}
function renderMeta(r) {
  const fmt = v => 'R$ ' + Number(v||0).toLocaleString('pt-BR',{minimumFractionDigits:2});
  const pct = (r.percentual*100);
  const atingiu = pct >= 100;
  const cor = atingiu ? '#2e7d32' : '#e65100';
  document.getElementById('meta-kpis').innerHTML =
    _kpiCard('Meta', r.meta != null ? fmt(r.meta) : '—') +
    _kpiCard('Realizado (vendido)', fmt(r.realizado)) +
    _kpiCard('% atingido', r.meta != null ? pct.toFixed(1) + '%' : '—', cor);
  const aviso = r.meta == null ? '<div style="font-size:12px;color:#e65100;margin-bottom:8px">Defina a meta do mês.</div>' : '';
  const w = Math.min(100, Math.max(0, pct));
  document.getElementById('meta-barra').innerHTML = aviso + `
    <div style="background:#f0f0f0;border-radius:8px;height:14px;overflow:hidden">
      <div style="background:${cor};height:14px;width:${w}%"></div>
    </div>`;
}
async function salvarMetaUI() {
  const ano = parseInt(document.getElementById('meta-ano').value);
  const mes = parseInt(document.getElementById('meta-mes').value);
  const valor = parseFloat(document.getElementById('meta-valor').value);
  if (!(valor >= 0)) { showToast('Informe um valor de meta válido'); return; }
  const res = await api('/api/v2/analises/metas', { method:'POST', body: JSON.stringify({ ano, mes, valor }) });
  if (res?.errors || res?.error) { showToast('Erro: ' + (res.errors?.[0]||res.error)); return; }
  showToast('✅ Meta salva');
  loadMeta();
}
```
(Confirmar o nome real do helper de toast — `showToast` — lendo o arquivo.)

- [ ] **Step 4: Deploy + verificação**

```bash
rsync -az public/dashboard.html root@2.25.147.243:/var/www/lkl-chatbot/public/dashboard.html
```
No painel: aba Análises → pill Metas; salvar uma meta do mês; conferir cards (Meta/Realizado/%) + barra; trocar de mês recarrega. Reportar.

- [ ] **Step 5: Commit**

```bash
git add public/dashboard.html
git commit -m "feat(analises): pill Metas + bloco (meta mensal x realizado vendido + progresso)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Atualizar memória do projeto

**Files:**
- Modify: `/Users/klebercamara/.claude/projects/-Users-klebercamara-LKL/memory/project_sprint_status.md`

- [ ] **Step 1: Registrar**

Marcar "AG-3 Metas: CONCLUÍDO — 2026-06-24": tabela metas (migration 039, UNIQUE ano,mes); módulo analises salvarMeta (upsert) / metaMes (realizado = SUM(orcamentos.total) WHERE aprovado_em no mês AND status NOT IN cancelado/reprovado; percentual); rotas GET/POST /analises/metas. Frontend: pill Metas na sub-nav (#ag-metas) com seletor mês/ano + input meta + cards Meta/Realizado/% + barra de progresso. Próxima migration livre: 040. Resta AG-4 Insights IA (precisa ANTHROPIC_API_KEY no .env do VPS).

- [ ] **Step 2: Sem commit** (memória fora do git).

---

## Notas de verificação final

- `aprovado_em` é setado quando o orçamento é aprovado; orçamentos aprovados antes desse campo existir podem ter `aprovado_em` nulo (não entram no realizado) — aceitável.
- Percentual exibido com 1 casa; valores monetários `toLocaleString('pt-BR',{minimumFractionDigits:2})`.
